to setup project first you have install all dependencies with the below command line

### `npm install or npm i`

In the project directory, you can run:

### `npm start`

for app test cases, you can run:

### `npm test`

